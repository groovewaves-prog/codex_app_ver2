/**
 * Phase 8 段階 8-Z: モック計画書 DOCX 生成
 *
 * 仮想ドメイン: 社内クラウド統合認証システム (HikariAuth、架空)
 * 構造定義書 v0.2 の全 15 章を 1 ファイルに集約。
 * 各章に「明らかな不足」を埋め込み (LLM レビューでの指摘期待)。
 *
 * 注: ユーザは MS-Word でこの .docx を開き、自身で PDF 変換する想定。
 */

const fs = require('fs');
const {
  Document, Packer, Paragraph, TextRun, HeadingLevel,
  AlignmentType, PageBreak,
} = require('docx');

// ============================================================================
// モック計画書本体: 15 章を 1 ファイルに
// ============================================================================
const CHAPTERS = [
  // --------------------------------------------------------------------------
  // 第 1 章 はじめに
  // --------------------------------------------------------------------------
  {
    title: "第 1 章 はじめに",
    sections: [
      {
        heading: "1.1 本書の目的",
        body: "本書は、社内クラウド統合認証システム「HikariAuth」(仮称) の基本設計を示すものである。"
            + "既存のオンプレ Active Directory を AWS 上のクラウド認証基盤に移行することを目的とする。"
      },
      // ⚠️ 不足: 想定読者・対象範囲が記載なし
      {
        heading: "1.2 スコープ",
        body: "本システムは社員 1,200 名を対象とした SAML / OIDC 統合認証を提供する。"
            + "今後 3 年間で社内 SaaS 12 種を統合する計画である。"
      },
      // ⚠️ 不足: 対象外範囲 (社外パートナーは対象外か等) が記載なし
    ],
  },
  // --------------------------------------------------------------------------
  // 第 2 章 システム要件
  // --------------------------------------------------------------------------
  {
    title: "第 2 章 システム要件",
    sections: [
      {
        heading: "2.1 機能要件",
        body: "SAML 2.0 / OIDC による SSO、MFA (TOTP)、パスワードリセット、"
            + "アカウントライフサイクル管理 (入社・退職・異動)、監査ログを提供する。"
      },
      {
        heading: "2.2 非機能要件",
        body: "稼働時間は平日 8 時から 22 時を想定。レスポンスは 3 秒以内を目標とする。"
      },
      // ⚠️ 不足: 可用性目標 SLA (例: 99.9%) が未定義、データ保護要件も不明
    ],
  },
  // --------------------------------------------------------------------------
  // 第 3 章 システム全体構成
  // --------------------------------------------------------------------------
  {
    title: "第 3 章 システム全体構成",
    sections: [
      {
        heading: "3.1 物理構成",
        body: "AWS Tokyo リージョンを利用する。アプリケーション層 (ECS Fargate)、"
            + "データ層 (RDS PostgreSQL)、キャッシュ層 (ElastiCache Redis) で構成する。"
      },
      // ⚠️ 不足: AZ 配置・冗長性 (Multi-AZ か Single-AZ か) の言及なし
      {
        heading: "3.2 論理構成",
        body: "認証フロントエンドが ALB 経由でアクセスを受け、内部 API が RDS と Redis を参照する。"
      },
    ],
  },
  // --------------------------------------------------------------------------
  // 第 4 章 ネットワーク設計
  // --------------------------------------------------------------------------
  {
    title: "第 4 章 ネットワーク設計",
    sections: [
      {
        heading: "4.1 VPC 構成",
        body: "本番環境用に VPC を 1 つ作成する。Public / Private サブネットを分離する。"
      },
      // ⚠️ 不足: サブネット CIDR の具体的な値が記載なし、ルーティング戦略不明
      {
        heading: "4.2 接続性",
        body: "オンプレ AD との間は Site-to-Site VPN で接続する。帯域は 100 Mbps を想定。"
      },
    ],
  },
  // --------------------------------------------------------------------------
  // 第 5 章 アカウント・認可設計
  // --------------------------------------------------------------------------
  {
    title: "第 5 章 アカウント・認可設計",
    sections: [
      {
        heading: "5.1 認証方式",
        body: "SAML 2.0 を主体とし、対応していない SaaS のみ OIDC を併用する。"
      },
      {
        heading: "5.2 権限分離",
        body: "管理者ロール、運用ロール、一般ユーザロールに分ける。"
      },
      // ⚠️ 不足: RBAC か ABAC か、選定理由が記載なし、最小権限の原則の言及なし
    ],
  },
  // --------------------------------------------------------------------------
  // 第 6 章 データ設計
  // --------------------------------------------------------------------------
  {
    title: "第 6 章 データ設計",
    sections: [
      {
        heading: "6.1 データモデル",
        body: "ユーザマスタ、認証ログ、セッション情報を PostgreSQL に保持する。"
            + "セッション情報のみ Redis にキャッシュする。"
      },
      // ⚠️ 不足: データ暗号化 (at-rest, in-transit) の方針が記載なし
      {
        heading: "6.2 バックアップ",
        body: "RDS のスナップショットを毎日取得する。"
      },
      // ⚠️ 不足: バックアップ世代数・保持期間が未定義
    ],
  },
  // --------------------------------------------------------------------------
  // 第 7 章 アプリケーション・サービス設計
  // --------------------------------------------------------------------------
  {
    title: "第 7 章 アプリケーション・サービス設計",
    sections: [
      {
        heading: "7.1 サービス構成",
        body: "認証 API、管理 API、Webhook 配信、バッチ処理の 4 サービスで構成する。"
            + "Node.js + TypeScript で実装する。"
      },
      {
        heading: "7.2 サービス間通信",
        body: "サービス間は REST API で連携する。"
      },
      // ⚠️ 不足: API 認証方式 (OAuth2 / JWT / mTLS) が選定されていない
    ],
  },
  // --------------------------------------------------------------------------
  // 第 8 章 可用性設計
  // --------------------------------------------------------------------------
  {
    title: "第 8 章 可用性設計",
    sections: [
      {
        heading: "8.1 可用性目標",
        body: "本番運用時は高可用性を確保する。"
      },
      // ⚠️ 不足: SLI/SLO の具体的数値、RTO/RPO の目標値が未記載 (重大)
      {
        heading: "8.2 DR 対応",
        body: "災害発生時に備えた対応を検討する。"
      },
      // ⚠️ 不足: DR サイト構成、フェイルオーバ手順、訓練計画なし
    ],
  },
  // --------------------------------------------------------------------------
  // 第 9 章 性能設計
  // --------------------------------------------------------------------------
  {
    title: "第 9 章 性能設計",
    sections: [
      {
        heading: "9.1 性能要件",
        body: "ピーク時の同時接続数は 500 セッションを想定。"
            + "認証処理は十分なレスポンス速度を提供する。"
      },
      // ⚠️ 不足: 性能 KPI (RPS、レイテンシ p95/p99) の具体的数値なし
      {
        heading: "9.2 スケーラビリティ",
        body: "需要増加時には ECS タスク数を自動でスケールアウトする。"
      },
      // ⚠️ 不足: スケールアウト閾値、上限が未定義
    ],
  },
  // --------------------------------------------------------------------------
  // 第 10 章 セキュリティ設計
  // --------------------------------------------------------------------------
  {
    title: "第 10 章 セキュリティ設計",
    sections: [
      {
        heading: "10.1 セキュリティ方針",
        body: "認証情報の漏洩防止を最優先とする。Web Application Firewall を導入する。"
      },
      // ⚠️ 不足: 脅威モデル (STRIDE / DREAD 等) の実施が記載なし
      {
        heading: "10.2 監査",
        body: "認証ログを記録し、定期的にレビューする。"
      },
      // ⚠️ 不足: 監査ログの改ざん防止策、SIEM 連携の有無が未定
    ],
  },
  // --------------------------------------------------------------------------
  // 第 11 章 運用設計
  // --------------------------------------------------------------------------
  {
    title: "第 11 章 運用設計",
    sections: [
      {
        heading: "11.1 監視",
        body: "Amazon CloudWatch でメトリクスを収集する。異常時はアラートを送信する。"
      },
      // ⚠️ 不足: アラート閾値、連絡体制 (オンコール、エスカレーション) が未定義
      {
        heading: "11.2 デプロイ",
        body: "CI/CD で自動デプロイする。"
      },
      // ⚠️ 不足: デプロイ戦略 (Blue/Green、Canary 等) が未選定
    ],
  },
  // --------------------------------------------------------------------------
  // 第 12 章 コスト設計
  // --------------------------------------------------------------------------
  {
    title: "第 12 章 コスト設計",
    sections: [
      {
        heading: "12.1 コスト方針",
        body: "コスト最適化を継続的に実施する。"
            + "Reserved Instance や Savings Plans の活用を検討する。"
      },
      // ⚠️ 不足: 月額・年額コスト見積りの概算すらなし、TCO 比較もなし
    ],
  },
  // --------------------------------------------------------------------------
  // 第 13 章 拡張性・保守性設計
  // --------------------------------------------------------------------------
  {
    title: "第 13 章 拡張性・保守性設計",
    sections: [
      {
        heading: "13.1 拡張性",
        body: "新規 SaaS 統合を容易にする設計とする。"
      },
      // ⚠️ 不足: 具体的な拡張ポイント (プラグイン機構、設定外出し等) が未設計
      {
        heading: "13.2 保守性",
        body: "コード品質を維持する。"
      },
      // ⚠️ 不足: ベンダロックイン回避策、保守要員のスキルセット要件未記載
    ],
  },
  // --------------------------------------------------------------------------
  // 第 14 章 移行設計
  // --------------------------------------------------------------------------
  {
    title: "第 14 章 移行設計",
    sections: [
      {
        heading: "14.1 移行方針",
        body: "既存 AD からの段階移行を行う。並行稼働期間を 3 ヶ月設ける。"
      },
      {
        heading: "14.2 移行手順",
        body: "ユーザマスタを段階的に移行する。"
      },
      // ⚠️ 不足: ロールバック手順 (失敗時の戻し方) が未定義、データ整合性検証手順なし
    ],
  },
  // --------------------------------------------------------------------------
  // 第 15 章 補足
  // --------------------------------------------------------------------------
  {
    title: "第 15 章 補足",
    sections: [
      {
        heading: "15.1 前提条件",
        body: "本書はオンプレ AD の置き換えを前提としている。"
            + "他システム連携については別途設計書を作成する。"
      },
      {
        heading: "15.2 参照文書",
        body: "・社内クラウド利用ガイドライン v3.0\n・情報セキュリティ規程"
      },
      // ⚠️ 不足: 改訂履歴 (バージョン、変更日、変更者) が未記載
      // ⚠️ 不足: リスク管理 (識別済みリスク、対応策) が記載なし
    ],
  },
];


// ============================================================================
// DOCX 生成
// ============================================================================

const children = [];

// タイトルページ
children.push(
  new Paragraph({
    children: [new TextRun({ text: "社内クラウド統合認証システム", bold: true, size: 36, font: "Yu Gothic" })],
    alignment: AlignmentType.CENTER,
    spacing: { after: 200 },
  })
);
children.push(
  new Paragraph({
    children: [new TextRun({ text: "HikariAuth 基本設計書", bold: true, size: 32, font: "Yu Gothic" })],
    alignment: AlignmentType.CENTER,
    spacing: { after: 600 },
  })
);
children.push(
  new Paragraph({
    children: [new TextRun({ text: "(モック計画書 — レビュー検証用)", size: 22, font: "Yu Gothic" })],
    alignment: AlignmentType.CENTER,
    spacing: { after: 100 },
  })
);
children.push(
  new Paragraph({
    children: [new TextRun({ text: "Phase 8 段階 8-Z 検証用ファイル", size: 22, font: "Yu Gothic" })],
    alignment: AlignmentType.CENTER,
    spacing: { after: 200 },
  })
);
children.push(
  new Paragraph({ children: [new PageBreak()] })
);

// 各章
for (const chapter of CHAPTERS) {
  children.push(
    new Paragraph({
      children: [new TextRun({ text: chapter.title, bold: true, size: 28, font: "Yu Gothic" })],
      heading: HeadingLevel.HEADING_1,
      spacing: { before: 240, after: 120 },
    })
  );
  for (const section of chapter.sections) {
    children.push(
      new Paragraph({
        children: [new TextRun({ text: section.heading, bold: true, size: 24, font: "Yu Gothic" })],
        heading: HeadingLevel.HEADING_2,
        spacing: { before: 160, after: 80 },
      })
    );
    // body の改行を分割
    const lines = section.body.split("\n");
    for (const line of lines) {
      children.push(
        new Paragraph({
          children: [new TextRun({ text: line, size: 20, font: "Yu Gothic" })],
          spacing: { after: 80 },
        })
      );
    }
  }
}

const doc = new Document({
  creator: "Phase 8 段階 8-Z モック生成",
  title: "HikariAuth 基本設計書 (モック)",
  description: "構造定義書 v0.2 の全 15 章を含むレビュー検証用モック計画書",
  sections: [
    {
      properties: {
        page: {
          margin: { top: 1134, right: 1134, bottom: 1134, left: 1134 }, // 約 20mm
        },
      },
      children,
    },
  ],
});

const outputPath = "/mnt/user-data/outputs/mock_基本設計書_HikariAuth_v1.docx";

Packer.toBuffer(doc).then(buffer => {
  fs.writeFileSync(outputPath, buffer);
  console.log(`✓ DOCX 生成完了: ${outputPath}`);

  // 文字数集計
  let totalChars = 0;
  for (const ch of CHAPTERS) {
    for (const s of ch.sections) {
      totalChars += s.body.length;
    }
  }
  console.log(`本文文字数 (見出し除く): 約 ${totalChars} 字`);
  console.log(`章数: ${CHAPTERS.length}`);
});
