# secure_review runtime.
# Standard library is sufficient for the HTTP API. These dependencies are
# optional; they enable the Streamlit UI and native PDF extraction.
# Streamlit UI (primary UI as of 2026-04-23).
streamlit>=1.33
# PDF text extraction. Without this, the extractor falls back to `pdftotext`
# on PATH, and if that is missing it records a warning and skips.
pypdf>=4.0
# R-M experiment paused (2026-05-01): ginza/ja_ginza/spacy<3.8 caused a
# boot loop on Streamlit Cloud (Python 3.14) because spacy 3.7.x has no
# binary wheels for cp314 and the source build hangs. The Diagnostics
# expander in streamlit_app.py remains in place; it surfaces an ImportError
# in its own try/except when GiNZA is not installed and does not affect the
# rest of the UI. Future R-M work should either (a) switch to spacy-official
# ja_core_news_md, which tracks current spacy releases, or (b) drop NER and
# rely on regex/heuristics + an external API for proper-noun classification.
